# .env.example for FairTrade Infra Benchmarks

# ---------------------------------------------------------------------------
# Core Optimism Sepolia configuration
# ---------------------------------------------------------------------------

# Primary RPC endpoint used by most scripts (deploy, faucets, batch ops, etc.)
OP_SEPOLIA_RPC_URL=https://opt-sepolia.g.alchemy.com/v2/YOUR_ALCHEMY_KEY_HERE

# Private key of the main benchmarking/deployer wallet (testnet only!)
# NEVER commit a real key. Replace this placeholder with your own.
OP_SEPOLIA_PRIVATE_KEY=0xYOUR_TEST_PRIVATE_KEY_HERE

# Convenience: address corresponding to OP_SEPOLIA_PRIVATE_KEY
OP_SEPOLIA_ADDRESS=0xYourTestWalletAddressHere

# ---------------------------------------------------------------------------
# RPC endpoint sets (used by benchmark scripts)
# ---------------------------------------------------------------------------

# Public / shared Optimism Sepolia RPCs
# NOTE: value must be a JSON array encoded as a single string.
OP_SEPOLIA_PUBLIC_RPCS_JSON='["https://api.zan.top/opt-sepolia","https://optimism-sepolia.drpc.org","https://optimism-sepolia-public.nodies.app","https://optimism-sepolia.gateway.tenderly.co","https://endpoints.omniatech.io/v1/op/sepolia/public","https://sepolia.optimism.io","https://optimism-sepolia.api.onfinality.io/public","https://optimism-sepolia-testnet.api.pocket.network","https://optimism-sepolia.therpc.io","https://public.stackup.sh/api/v1/node/optimism-sepolia"]'

# Private / rate-limit-friendly RPCs (Infura, Alchemy, Chainstack, etc.)
OP_SEPOLIA_PRIVATE_RPCS_JSON='["https://optimism-sepolia.infura.io/v3/YOUR_INFURA_KEY_HERE","https://opt-sepolia.g.alchemy.com/v2/YOUR_ALCHEMY_KEY_HERE","https://optimism-sepolia.core.chainstack.com/YOUR_CHAINSTACK_KEY_HERE"]'

# ---------------------------------------------------------------------------
# Optional: contract overrides (only needed if you redeploy)
# ---------------------------------------------------------------------------

# If you redeploy ActorRegistry, you can override its address here so
# register-actors-from-faucet.ts picks it up without code changes.
# ACTOR_REGISTRY_ADDRESS=0xFb451B3Bfb497C54719d0DB354a502a9D9cE38C1

# ---------------------------------------------------------------------------
# Optional: faucet distribution tuning (scripts/distribute-op-faucet.ts)
# ---------------------------------------------------------------------------

# Minimum balance to keep on the funder wallet as a safety buffer (in ETH)
# GAS_BUFFER_WEI is set in code from this value.
# DISTRIBUTE_GAS_BUFFER_ETH=0.005

# Number of wallets to generate/fund in the faucet file
# TARGET_WALLET_COUNT=50

# Delay (ms) between txs to avoid hammering RPCs
# PER_TX_DELAY_MS=300

# ---------------------------------------------------------------------------
# Optional: actor registration tuning (scripts/register-actors-from-faucet.ts)
# ---------------------------------------------------------------------------

# Minimum balance (in ETH) a wallet must have before being registered
# REGISTER_MIN_BALANCE_ETH=0.0000001

# RPC concurrency caps (kept at 1 by default to be gentle to RPCs)
# REGISTER_BALANCE_CONCURRENCY=1
# REGISTER_REGISTER_CONCURRENCY=1

# ---------------------------------------------------------------------------
# Optional: CID batch TPS benchmarking (scripts/benchmark-cid-batch.ts)
# ---------------------------------------------------------------------------

# Number of CID events per submitCidBatch call
# BATCH_SIZE=200

# How long to keep sending txs in the benchmark (ms)
# DURATION_MS=30000

# Workers per RPC endpoint
# WORKERS_PER_RPC=3

# Run label used to namespace product/step IDs
# CID_RUN_ID=local-dev

# Path to faucet wallets state file (for multiwallet scripts)
# FAUCET_STATE_FILE=./op-sepolia-faucet-wallets-batch.json

# ---------------------------------------------------------------------------
# Optional: CID batch size limit search (scripts/estimate-cid-batch-limit.ts)
# ---------------------------------------------------------------------------

# Global min/max search range for batch size
# CID_MIN_BATCH=100
# CID_MAX_BATCH=4096

# Initial probe size for binary search
# CID_INITIAL_BATCH=800

# Additional tag for uniqueness across runs
# CID_RUN_ID=cid-batch-limit-local

# Delay (ms) between trials to avoid rate limits
# CID_TRIAL_DELAY_MS=10000

# ---------------------------------------------------------------------------
# Optional: RPC RPS benchmark (scripts/benchmark-op-rpcs.ts)
# ---------------------------------------------------------------------------

# TX_COUNT_PER_RPC=60
# RPC_BENCH_CONCURRENCY=20
# RPC_BENCH_GAS_BUFFER_ETH=0.05
